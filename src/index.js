const mk = require('./mk.js');
const clone = require('./clone.js');
const read = require('./read.js');
const rm = require('./rm.js');

/**
 * @module
 */
module.exports = {
  mk: mk,
  make: mk,
  clone: clone,
  read: read,
  rm: rm,
  remove: rm,
};
